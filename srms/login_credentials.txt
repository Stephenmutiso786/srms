No demo accounts are included for production use.

Create your first admin via the setup page:

- Set an environment variable `SETUP_TOKEN` on your server
- Open: `/setup?token=YOUR_SETUP_TOKEN`

After the admin is created, login with the email/password you set.
